<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>API Dashboard - Home</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

</head>

<body class="bg-gray-900 min-h-screen p-8">

    <div class="absolute inset-0 bg-gradient-to-br from-indigo-900 via-gray-900 to-purple-900 opacity-70"></div>

    <div class="z-10 relative max-w-7xl mx-auto space-y-8">

        <header class="flex flex-col sm:flex-row justify-between items-start sm:items-center">

            <div class="mb-4 sm:mb-0">
                <p class="text-lg text-indigo-400 font-medium">
                    Welcome back, <span class="font-bold">
                        <%= user.username || '' %>
                    </span>
                </p>
                <h1 class="text-4xl font-bold text-white">
                    Developer Dashboard
                </h1>
            </div>

            <form action="/api/generate" method="post">
                <div class="flex space-x-4">
                    <button
                        class="flex items-center space-x-2 py-2 px-4 bg-green-600 hover:bg-green-700 rounded-lg text-white font-semibold shadow-lg transition duration-300 ease-in-out focus:outline-none focus:ring-4 focus:ring-green-500/50">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                            <path fill-rule="evenodd"
                                d="M10 3a1 1 0 011 1v5h5a1 1 0 110 2h-5v5a1 1 0 11-2 0v-5H4a1 1 0 110-2h5V4a1 1 0 011-1z"
                                clip-rule="evenodd" />
                        </svg>
                        <span>Generate API Key</span>
                    </button>
            </form>

            <form action="/auth/logout" method="post">
                <button type="submit"
                    class="flex items-center space-x-2 py-2 px-4 bg-red-600 hover:bg-red-700 rounded-lg text-white font-semibold shadow-lg transition duration-300 ease-in-out focus:outline-none focus:ring-4 focus:ring-red-500/50">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                        <path fill-rule="evenodd"
                            d="M3 3a1 1 0 00-1 1v12a1 1 0 001 1h10a1 1 0 001-1v-1.586l-2.293 2.293a1 1 0 01-1.414-1.414l4-4a1 1 0 011.414 0l4 4a1 1 0 01-1.414 1.414L15 14.414V16a1 1 0 01-1 1H3a1 1 0 01-1-1V4a1 1 0 011-1h10a1 1 0 011 1v1.586l-2.293-2.293a1 1 0 01-1.414 1.414l4 4a1 1 0 011.414 0l4-4a1 1 0 01-1.414-1.414L15 4.414V3a1 1 0 00-1-1H3zm12 8a1 1 0 100 2h-3a1 1 0 100-2h3z"
                            clip-rule="evenodd" />
                        <path d="M10 11a1 1 0 100-2H7a1 1 0 100 2h3z" />
                    </svg>
                    <span>Logout</span>
                </button>
            </form>
    </div>

    </header>

    <hr class="border-gray-700/50">

    <div class="bg-gray-800/30 backdrop-blur-xl border border-gray-700/50 rounded-xl shadow-2xl p-6">
        <h2 class="text-xl font-semibold text-white mb-4">API Key Status</h2>

        <% if(userAPI) { %>
            <div class="space-y-4">

                <div>
                    <label class="block text-sm font-medium text-gray-400 mb-1">Your Current API Key</label>
                    <div class="flex items-center space-x-2 bg-gray-700/50 rounded-lg p-3">
                        <code id="apiKey" class="flex-grow text-white truncate">
                            <%= userAPI.key %>
                        </code>
                        <button class="text-indigo-400 hover:text-indigo-300 text-sm font-medium">Copy</button>
                    </div>
                </div>

                <div class="flex justify-between text-sm text-gray-400">
                    <span>Status: <span class="text-green-400 font-medium">Active</span></span>
                    <span>Created: <%= new Date(userAPI.createdAt).toLocaleString('en-Us', { month: "short" ,
                            day: "numeric" , year: "numeric" }) %></span>
                    <span>Expires: Never</span>
                </div>
            </div>

            <% } else {%>

                <div class="space-y-4 text-center py-8">

                    <svg xmlns="http://www.w3.org/2000/svg" class="h-10 w-10 text-yellow-400 mx-auto" fill="none"
                        viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                        <path stroke-linecap="round" stroke-linejoin="round"
                            d="M15 7a2 2 0 012 2v5l-2.293 2.293a1 1 0 01-1.414 0L12 14.586V13a1 1 0 00-1-1H9a1 1 0 00-1 1v1.586l-1.293 1.293a1 1 0 01-1.414 0L5 14.586V9a2 2 0 012-2h8z" />
                    </svg>
                    <p class="text-lg font-medium text-white">No Active API Key Found</p>
                    <p class="text-gray-400 max-w-sm mx-auto">You haven't generated an API key yet. Click the button
                        below to get started and unlock the API access.</p>

                    <form action="/api/generate" method="post">
                        <button type="submit"
                            class="mt-4 inline-flex items-center space-x-2 py-2 px-6 bg-indigo-600 hover:bg-indigo-700 rounded-lg text-white font-semibold shadow-lg transition duration-300 ease-in-out focus:outline-none focus:ring-4 focus:ring-indigo-500/50">
                            <span>Generate API Key</span>
                        </button>
                    </form>
                </div>

                <% } %>

    </div>

    <div class="bg-gray-800/30 backdrop-blur-xl border border-gray-700/50 rounded-xl shadow-2xl p-6">
        <h2 class="text-xl font-semibold text-white mb-4">Route Usage Tracking</h2>

        <% if(userAPI && userAPI.track.length !==0) { %>

            <div class="overflow-x-auto">
                <table class="min-w-full divide-y divide-gray-700/50">
                    <thead>
                        <tr>
                            <th class="px-4 py-3 text-left text-xs font-medium text-gray-400 uppercase tracking-wider">
                                Route
                            </th>
                            <th class="px-4 py-3 text-left text-xs font-medium text-gray-400 uppercase tracking-wider">
                                Method
                            </th>
                            <th class="px-4 py-3 text-right text-xs font-medium text-gray-400 uppercase tracking-wider">
                                Total Calls
                            </th>
                            <th class="px-4 py-3 text-right text-xs font-medium text-gray-400 uppercase tracking-wider">
                                Usage Rate
                            </th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-gray-700/50 text-white">
                        <% userAPI.track.forEach((tr)=> { %>
                            <tr class="hover:bg-gray-700/20 transition duration-150">
                                <td class="px-4 py-4 whitespace-nowrap text-sm font-medium text-indigo-300">
                                    <%= tr.url %>
                                </td>
                                <td
                                    class="px-4 py-4 whitespace-nowrap text-sm <%- (tr.method.toUpperCase() == 'POST') ? 'text-green-400' : 'text-yellow-400' %>">
                                    <%= tr.method.toUpperCase() %>
                                </td>
                                <td class="px-4 py-4 whitespace-nowrap text-sm text-right">
                                    <%= tr.count %>
                                </td>
                                <td class="px-4 py-4 whitespace-nowrap text-sm text-right text-green-400">
                                    <%= ((tr.count/userAPI.totalCounts)*100).toFixed(2) %>%
                                </td>
                            </tr>

                            <% }) %>
                    </tbody>
                </table>
            </div>
            <% } else { %>

                <div class="text-center py-10">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-10 w-10 text-gray-500 mx-auto mb-3" fill="none"
                        viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                        <path stroke-linecap="round" stroke-linejoin="round"
                            d="M9 19V6a2 2 0 00-2-2H5a2 2 0 00-2 2v13a2 2 0 002 2h4a2 2 0 002-2zm0 0l-1.5-1.5M15 13V6a2 2 0 012-2h2a2 2 0 012 2v7m-7 0l-1.5-1.5M18 10h.01M18 14h.01M18 18h.01" />
                    </svg>
                    <p class="text-lg font-medium text-gray-300">No API Usage Recorded</p>
                    <p class="text-gray-500">Usage statistics will appear here after you generate your API key and make
                        your first successful API call.</p>
                </div>

                <div class="overflow-x-auto hidden">
                    <table class="min-w-full divide-y divide-gray-700/50">
                        <thead>
                            <tr>
                                <th
                                    class="px-4 py-3 text-left text-xs font-medium text-gray-400 uppercase tracking-wider">
                                    Route</th>
                                <th
                                    class="px-4 py-3 text-left text-xs font-medium text-gray-400 uppercase tracking-wider">
                                    Method</th>
                                <th
                                    class="px-4 py-3 text-right text-xs font-medium text-gray-400 uppercase tracking-wider">
                                    Total Calls</th>
                                <th
                                    class="px-4 py-3 text-right text-xs font-medium text-gray-400 uppercase tracking-wider">
                                    Success Rate</th>
                            </tr>
                        </thead>
                        <tbody class="divide-y divide-gray-700/50 text-white">
                        </tbody>
                    </table>
                </div>

                <% } %>


    </div>
    </div>

</body>

</html>